---
description: 
globs: 
alwaysApply: true
---

# Your rule content

- You can @ files here
- You can use markdown but dont have to

- 꼭 필요한 인터페이스가 아닌 경우 인터페이스 - impl 분리는 지양합니다.
- Spring bean 의존성 주입은 롬복을 통해 @RequiredArgsConstructor 방식을 사용합니다.
- 외부 API 를 호출하는 기능은 OpenFeign 을 사용합니다.
- 객체 생성은 복잡한 경우 (필드가 4개 이상) 롬복의 builder 를 사용합니다.
- Entity 의 ID 는 Long 타입으로 합니다.
- Jpa 의 ID 생성은 @Generated(strategy = IDENTITY) 로 합니다.
- Request, Response, 등등 DTO 는 별도의 클래스로 구성합니다.